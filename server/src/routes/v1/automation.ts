import type { FastifyInstance } from 'fastify';

/**
 * Automation: triggers (campaigns), routing rules, and bot flows.
 *
 * Registered from index.ts so the surface exists as a seam before it is filled.
 */
export async function automationV1Routes(_app: FastifyInstance): Promise<void> {
  // Phase 11.
}
